<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        Title="Fichiers liés au projet"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Width="500" Height="600">
  <DockPanel Margin="10">
    <TextBox Name="txtDump"
             FontFamily="Consolas"
             FontSize="12"
             AcceptsReturn="True"
             AcceptsTab="True"
             TextWrapping="Wrap"
             VerticalScrollBarVisibility="Auto"
             HorizontalScrollBarVisibility="Auto"
             IsReadOnly="True"
             DockPanel.Dock="Top"/>
    <!-- Fenetre a bouton unique : "Fermer" prend le style Appliquer sur toute
         la largeur, puisqu'il est la seule action possible. -->
    <Button Content="Fermer" Name="btnClose"
            HorizontalAlignment="Stretch" DockPanel.Dock="Bottom"
            Margin="0,10,0,0"
            IsDefault="True"
            Style="{DynamicResource NMButtonAppliquer}"/>
  </DockPanel>
</Window>
