<?xml version="1.0" encoding="utf-8"?>

<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Liaison en cours"
        Height="155" Width="520"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="TextBlock" BasedOn="{StaticResource NMTextBlockStandard}" />
        <Style TargetType="Button"    BasedOn="{StaticResource NMButtonStandard}" />
    </Window.Resources>

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Compteur : "Liaison (x/total) :" -->
        <TextBlock x:Name="lblCounter"
                   Text="Liaison..."
                   FontSize="15"
                   FontWeight="SemiBold"
                   Grid.Row="0"/>

        <!-- Nom du fichier sur une ligne dédiée -->
        <TextBlock x:Name="lblMessage"
                   Text=""
                   FontSize="12"
                   TextTrimming="CharacterEllipsis"
                   ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                   Margin="0,2,0,0"
                   Grid.Row="1"/>

        <ProgressBar x:Name="progressBar"
                     Grid.Row="2"
                     Height="18"
                     Minimum="0"
                     Maximum="100"
                     Value="0"
                     Margin="0,8,0,0"/>

        <Button x:Name="btnCancel"
                Content="Annuler"
                Grid.Row="3"
                Width="120" Height="30"
				FontSize="18"
				VerticalContentAlignment="Center"
                HorizontalAlignment="Right"
                Margin="0,8,0,0"/>
    </Grid>
</Window>
