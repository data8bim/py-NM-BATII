<?xml version="1.0" encoding="utf-8"?>

<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Terminé"
		SizeToContent="WidthAndHeight"
		MinWidth="400"
		MaxWidth="450"
		MaxHeight="600"
        ResizeMode="NoResize"
		WindowStartupLocation="CenterScreen">  
  
	<Window.Resources>	
		<Style TargetType="Window" BasedOn="{StaticResource NMWindowStandard}" />
		<Style TargetType="TextBlock" BasedOn="{StaticResource NMTextBlockStandard}" />
		<Style TargetType="Button" BasedOn="{StaticResource NMButtonValide}" />
	</Window.Resources>
	
	<Grid Margin="5">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="10"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<!-- Titre -->
		<TextBlock x:Name="txtTitle"
				   Grid.Row="0"
				   FontSize="16"
				   FontWeight="Bold"
				   TextAlignment="Center"
				   Margin="5,5,5,8"/>

		<!-- Tableau des résultats -->
		<DataGrid x:Name="dataGrid"
				  Grid.Row="1"
				  AutoGenerateColumns="False"
				  IsReadOnly="True"
				  HeadersVisibility="Column"
				  CanUserAddRows="False"
				  CanUserDeleteRows="False"
				  CanUserResizeRows="False"
				  CanUserSortColumns="False"
				  RowHeaderWidth="0"
				  GridLinesVisibility="Horizontal"
				  BorderBrush="DodgerBlue"
				  BorderThickness="1"
				  Margin="5">

			<DataGrid.RowStyle>
				<Style TargetType="DataGridRow">
					<Style.Triggers>
						<DataTrigger Binding="{Binding [IsTotal]}" Value="1">
							<Setter Property="FontWeight" Value="Bold"/>
						</DataTrigger>
						<DataTrigger Binding="{Binding [IsSep]}" Value="1">
							<Setter Property="FontWeight" Value="Bold"/>
							<Setter Property="Focusable" Value="False"/>
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</DataGrid.RowStyle>

			<DataGrid.Columns>
				<DataGridTextColumn Header="Catégorie"
									Binding="{Binding [Categorie]}"
									Width="*"/>
				<DataGridTextColumn Header="Nombre"
									Binding="{Binding [Nombre]}"
									Width="80">
					<DataGridTextColumn.ElementStyle>
						<Style TargetType="TextBlock">
							<Setter Property="HorizontalAlignment" Value="Right"/>
							<Setter Property="Margin" Value="0,0,8,0"/>
						</Style>
					</DataGridTextColumn.ElementStyle>
				</DataGridTextColumn>
				<DataGridTextColumn Binding="{Binding [IsTotal]}" Visibility="Collapsed"/>
				<DataGridTextColumn Binding="{Binding [IsSep]}"   Visibility="Collapsed"/>
			</DataGrid.Columns>
		</DataGrid>

		<Button x:Name="btnClose"
				Content="Fermer"
				Grid.Row="3"
				HorizontalAlignment="Stretch"
				IsDefault="True"/>

	</Grid>
</Window>
